# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Pose model (large ~30 MB; download separately -- see backend/models/README.md)
*.task
backend/models/*.task

# Node (future frontend)
node_modules/
.next/
dist/
build/

# Env / secrets
.env
.env.*
!.env.example
secrets/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local media -- input clips + generated overlays (keep the repo code-only)
*.mp4
*.MP4
*.mov
*.MOV
*.avi
# ...except the landing page's hero clip, which is a product asset the app
# serves, not a local recording lying around.
!backend/app/static/media/hero.mp4

# Local SQLite DB (dev only; prod uses Postgres)
*.db
*.sqlite3

# Local outputs / scratch
*.log
uploads/
overlay_videos/

# Landing-page source material: full-size originals and saved result pages the
# assets in backend/app/static/media were derived from. Tens of MB, and the
# derived versions are what the app actually serves.
compare/

# Scratch folder for clips and reports handed over for debugging
upload/

# Claude Code session-local permission grants (absolute machine paths)
.claude/settings.json
.claude/settings.local.json
