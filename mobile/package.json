{
  "name": "flapp-mobile",
  "private": true,
  "version": "0.1.0",
  "description": "Flapp iOS/Android shell (Capacitor). Analysis stays server-side; the SPA is bundled from backend/app/static by scripts/sync-www.mjs.",
  "scripts": {
    "check:native": "node scripts/check-native.mjs",
    "sync:www": "node scripts/sync-www.mjs",
    "sync:www:local": "node scripts/sync-www.mjs --api-base http://127.0.0.1:8099",
    "sync": "npm run sync:www && npx cap sync android",
    "android": "npm run sync && npx cap open android",
    "ios:ci": "npm run sync:www && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/browser": "^7.0.0",
    "@capacitor/camera": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/preferences": "^7.0.0",
    "@capacitor/share": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
